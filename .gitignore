# Node
node_modules/

# Salida del build. Anclado a la raiz: un "dist/" suelto tambien ignora
# vendor/reveal/dist/ y el deck queda sin reveal.js.
# index.html SI se commitea: es lo que hace que ande con doble clic, sin build.
/dist/

# OS
.DS_Store

# Verilator
obj_dir/
# La cache de g++ del contenedor: CCACHE_DIR=/work/.ccache. Ver docs/docker.md.
.ccache/
coverage.dat
*.vcd
code/.uvm/
